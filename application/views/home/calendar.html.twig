{#
    Filename    : calendar.html.twig
    Location    : application/views/home/calendar.html.twig
    Purpose     : Calendar partial page
    Created     : 06/03/2019 19:32:12 by Spiderman
    Updated     : 10/25/2019 19:32:09 by Spiderman
    Changes     : 
#}

{% extends 'base.html.twig' %}
{% block title %} {{ page_title }} | Carmel {% endblock %}
{% block contents %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-2">{{ page_title }}</h5>
                    <h4 class="card-subtitle">{{ page_subtitle }}</h4>
                </div>
                <div class="card-body">
                    <div id="fullcalendar"></div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block custom_scripts %}
    <script>
        $(function () {

            let baseURL = $.fn.baseURL();
            let apiURL = $.fn.apiURL();
            let branchID = $.fn.branchID();  

            let app = {
                calendar: function () {
                    $("#fullcalendar").fullCalendar({
                        header: {
                            left: "prev,next, today",
                            center: "title",
                            right: "month,agendaWeek,agendaDay,listMonth"
                        },
                        weekNumbers: true,
                        eventLimit: true,
                        editable: false,
                        events: "https://fullcalendar.io/demo-events.json"
                    });
                }
            };

            app.calendar();
        });
    </script>
{% endblock %}
